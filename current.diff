diff --git a/app/api/v1/auth.py b/app/api/v1/auth.py
index 9b88b28..489bd0c 100755
--- a/app/api/v1/auth.py
+++ b/app/api/v1/auth.py
@@ -415,7 +415,7 @@ def rotate_api_key(current_user, key_id):
         expires_in_days = None
         if old_key.expires_at:
             expires = old_key.expires_at
-            now = datetime.now(timezone.utc) if expires.tzinfo else datetime.utcnow()
+            now = datetime.now(timezone.utc) if expires.tzinfo else datetime.now(timezone.utc).replace(tzinfo=None)
             remaining = expires - now
             expires_in_days = max(1, remaining.days)
 
diff --git a/app/models_api_key.py b/app/models_api_key.py
index 880d143..f39abdf 100755
--- a/app/models_api_key.py
+++ b/app/models_api_key.py
@@ -141,10 +141,11 @@ class ApiKey(db.Model):
         if self.expires_at is None:
             return False
         # SQLite strips timezone info; handle both naive and aware datetimes
-        now = datetime.now(timezone.utc)
         expires = self.expires_at
         if expires.tzinfo is None:
-            now = datetime.utcnow()
+            now = datetime.now(timezone.utc).replace(tzinfo=None)
+        else:
+            now = datetime.now(timezone.utc)
         return expires < now
 
     def to_dict(self, include_key: bool = False) -> dict:
@@ -259,8 +260,9 @@ class RefreshToken(db.Model):
     def is_expired(self) -> bool:
         """Check if the refresh token has expired."""
         # SQLite strips timezone info; handle both naive and aware datetimes
-        now = datetime.now(timezone.utc)
         expires = self.expires_at
         if expires.tzinfo is None:
-            now = datetime.utcnow()
+            now = datetime.now(timezone.utc).replace(tzinfo=None)
+        else:
+            now = datetime.now(timezone.utc)
         return expires < now
