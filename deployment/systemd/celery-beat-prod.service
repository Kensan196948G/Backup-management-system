[Unit]
Description=Celery Beat - Production (Backup Management System)
After=network.target redis-server.service
Wants=redis-server.service

[Service]
Type=forking
User=www-data
Group=www-data
WorkingDirectory=/mnt/LinuxHDD/Backup-Management-System
EnvironmentFile=/mnt/LinuxHDD/Backup-Management-System/.env.production

# Python仮想環境のパス
Environment="PATH=/mnt/LinuxHDD/Backup-Management-System/venv/bin:/usr/local/bin:/usr/bin:/bin"

# Celery Beatの起動
ExecStart=/mnt/LinuxHDD/Backup-Management-System/venv/bin/python -m celery \
    -A celery_worker.celery_app beat \
    --loglevel=info \
    --logfile=/mnt/LinuxHDD/Backup-Management-System/logs/celery_beat_prod.log \
    --pidfile=/var/run/celery/celery_beat_prod.pid \
    --detach

# Celery Beatの停止
ExecStop=/bin/kill -TERM $MAINPID

# PIDファイルの場所
PIDFile=/var/run/celery/celery_beat_prod.pid

# 再起動設定
Restart=always
RestartSec=10s

# セキュリティ設定
NoNewPrivileges=true
PrivateTmp=true

[Install]
WantedBy=multi-user.target
