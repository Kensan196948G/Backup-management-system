# テスト実行コマンド

プロジェクトの全テストまたは特定のテストを実行します。

## 📋 概要

このコマンドは、pytestを使用してテストを実行し、結果をわかりやすく報告します。

## 🎯 テストタイプ

### 1. 全テスト実行

```bash
pytest tests/ -v
```

### 2. ユニットテストのみ

```bash
pytest tests/unit/ -v
```

### 3. 統合テストのみ

```bash
pytest tests/integration/ -v
```

### 4. 特定のファイル

```bash
pytest tests/test_backup.py -v
```

### 5. 特定のテストケース

```bash
pytest tests/test_backup.py::test_create_backup -v
```

## 📊 テストオプション

### カバレッジ付き実行

```bash
pytest --cov=app --cov-report=html --cov-report=term
```

### 並列実行（高速化）

```bash
pytest -n auto tests/
```

### 失敗時に即停止

```bash
pytest -x tests/
```

### 最後に失敗したテストのみ再実行

```bash
pytest --lf tests/
```

### 詳細な出力

```bash
pytest -vv --tb=long tests/
```

## 🚀 実行フロー

### ステップ1: 環境準備

```bash
# 仮想環境のアクティベート
source venv/bin/activate

# 依存関係の確認
pip install -r requirements.txt
```

### ステップ2: テスト実行

```bash
# テスト実行（並列）
pytest tests/ -v --cov=app --cov-report=html
```

### ステップ3: 結果の分析

テスト結果を分析し、以下を報告：
- 成功/失敗数
- カバレッジ率
- 失敗したテストの詳細
- 推奨アクション

## 📈 レポート形式

```markdown
# テスト実行結果

## サマリー
- **実行数**: 150件
- **成功**: 148件 ✅
- **失敗**: 2件 ❌
- **スキップ**: 0件
- **カバレッジ**: 82%

## 失敗したテスト

### 1. test_backup_with_invalid_path
- **ファイル**: tests/test_backup.py:45
- **エラー**: AssertionError: Expected exception not raised
- **原因**: パス検証ロジックの不備

### 2. test_restore_from_archive
- **ファイル**: tests/test_restore.py:78
- **エラー**: FileNotFoundError
- **原因**: テストデータが見つからない

## 推奨アクション

1. [ ] test_backup_with_invalid_pathの修正
2. [ ] test_restore_from_archiveのテストデータ作成
3. [ ] カバレッジを85%以上に向上
```

## 🔧 カスタマイズ

### pytest.ini 設定例

```ini
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    slow: marks tests as slow
    integration: marks tests as integration tests
    unit: marks tests as unit tests
```

### マーカーを使用したテスト選択

```bash
# ユニットテストのみ
pytest -m unit

# 統合テストのみ
pytest -m integration

# 遅いテスト以外
pytest -m "not slow"
```

## 💡 ベストプラクティス

1. **テストの分離**: ユニット/統合/機能テストを分ける
2. **高速実行**: 並列実行で時間短縮
3. **カバレッジ**: 80%以上を目標
4. **CI/CD統合**: 自動実行を設定

## 🔗 関連コマンド

- `/analyze` - コード品質分析
- `/commit` - テスト成功後のコミット
- `/pr` - テスト付きPR作成
